services:
  app:
    build: .
    ports:
    - "3000:3000"
    env_file:
      - .env
    environment:
      - DATABASE_URL=postgresql://postgres:postgres@db:5432/blog_api?schema=public
    depends_on:
      - db
  
  db:
    image: postgres:16-alpine
    restart: always
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=blog_api
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: